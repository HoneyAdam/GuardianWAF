# Hardcoded Secrets Scan Results

**Scan Date:** 2026-04-15
**Target:** GuardianWAF codebase (Go WAF + React dashboard)
**Scope:** API keys, private keys, passwords, tokens, connection strings, high-entropy secrets

---

## Summary

**No hardcoded production secrets found.**

The codebase follows security best practices:
- Session secrets are generated at runtime via `crypto/rand` (not hardcoded)
- API keys and passwords in config files are commented out with instructions to use environment variables
- Connection strings use localhost defaults with no embedded credentials
- Test files use only placeholder values (`test-key`, `secret123`, `common-password`, etc.)

---

## Findings

### [LOW] Hardcoded demo API key in example config

- **Category:** Hardcoded Secrets
- **Location:** `examples/standalone/guardianwaf.yaml:202`
- **Description:** Example configuration file contains a placeholder API key value `"guardianwaf-demo-2024"`.
- **Remediation:** This is an example/standalone config file intended for local demo purposes. The file already includes a comment `# Change this! Set a strong key`. No action required for production deployments when using this as a reference.

---

## Verified Safe Patterns

| Pattern | Status | Notes |
|---------|--------|-------|
| AWS/GitHub/Stripe keys | Clean | No live keys found; GitHub token references only in CI workflow templates using `${{ secrets.GITHUB_TOKEN }}` |
| Private keys | Clean | No RSA/EC/DSA/SSH private key headers in codebase |
| Database connection strings | Clean | No embedded credentials in mongo/postgres/redis URLs |
| Environment variable reads | Clean | All secrets properly loaded via `os.Getenv` / `process.env` |
| Test secrets | Clean | All test values are clearly marked as test placeholders (`test-key`, `secret123`, `changeme`) |

---

## Security Controls Observed

1. **Session management** (`internal/dashboard/auth.go`): Session secrets generated via `crypto/rand` at startup; survives restarts via optional `SetSessionSecret()` with hex decoding
2. **API key authentication**: Dashboard supports API key auth with `X-API-Key` header only (rejects query param to prevent log leakage)
3. **Password generation**: Dashboard password auto-generated via CSPRNG (`crypto/rand`) with fallback to hash-based entropy
4. **Config layering**: Production guide explicitly instructs using `${VARIABLE}` env var substitution for all secrets

---

*Generated by sc-secrets skill*
